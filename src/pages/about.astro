---
/**
 * About Me — short bio, full skill set, languages, education and contact.
 *
 * Contact is LinkedIn only. The phone number and email address from the CV are
 * intentionally not published anywhere on this site.
 */
import Base from '../layouts/Base.astro';
import PageHeader from '../components/PageHeader.astro';
import Icon from '../components/Icon.astro';
import { site, links, portrait } from '../data/site';
import { skillGroups, languages, education } from '../data/about';
import { yearsExperience } from '../data/experience';
import { url } from '../lib/url';
---

<Base
  title="About Me"
  description={`About ${site.name} — QA Engineer based in ${site.location}. Skills, languages, education and contact.`}
>
  <PageHeader
    eyebrow="About Me"
    title="The person<br />behind the bug reports"
    lead="QA Engineer based in Yerevan, working across mobile and PC games. Applied Mathematics graduate, occasional video editor, and permanently curious about how things break."
    meta={[site.location, `${yearsExperience}+ years experience`]}
  />

  <!-- Bio -->
  <section class="section">
    <div class="container bio">
      <div class="bio__portrait" data-reveal>
        {
          portrait ? (
            <img
              class="bio__photo"
              src={url(portrait)}
              alt={`${site.name}, ${site.role}`}
              width="900"
              height="1125"
            />
          ) : (
            /* Placeholder until a photo is added — see `portrait` in src/data/site.ts. */
            <div class="bio__placeholder" aria-hidden="true">
              <span class="bio__dots dots dots--ink" />
              <span class="bio__monogram">{site.initials}</span>
              <span class="bio__caption">{site.role}</span>
            </div>
          )
        }
      </div>

      <div class="bio__copy" data-reveal style="--reveal-delay:90ms">
        <p class="eyebrow">Hello</p>
        <h2 class="h2">I'm {site.name.split(' ')[0]}</h2>

        <div class="bio__text stack">
          <p>
            I'm a Quality Assurance Engineer in {site.location}. I spend my days making
            sure games hold together — designing and running test cases, chasing crashes
            through Firebase and Play Console, poking at APIs with Postman and Charles,
            and automating whatever is worth automating in Java.
          </p>
          <p>
            I came to QA from an engineering background: a Bachelor's in Applied
            Mathematics, then a year building GPU-accelerated tooling in C++, CUDA and
            LabVIEW at Ngene. That's why I'd rather write the internal tool than repeat
            the manual pass a fourth time.
          </p>
          <p>
            Outside of testing, I edit video. Most recently that meant 250+ mobile ad
            creatives and 50+ social videos for Melon Sandbox, a game with over 100
            million downloads — concept through to the final platform-ready cut.
          </p>
        </div>

        <a class="btn btn--outline" href={url('/qa')}>
          See what I've worked on
          <svg
            class="btn__arrow"
            width="15"
            height="15"
            viewBox="0 0 24 24"
            fill="none"
            stroke="currentColor"
            stroke-width="2.5"
            aria-hidden="true"
          >
            <path d="M5 12h14M13 5l7 7-7 7"></path>
          </svg>
        </a>
      </div>
    </div>
  </section>

  <!-- Skills -->
  <section class="section section--paper">
    <div class="container">
      <div class="section-head" data-reveal>
        <p class="eyebrow">Technical skills</p>
        <h2 class="h2">The full toolkit</h2>
      </div>

      <div class="grid-cards">
        {
          skillGroups.map((group, i) => (
            <div class="skill card" data-reveal style={`--reveal-delay:${i * 55}ms`}>
              <span class="skill__icon">
                <Icon name={group.icon} />
              </span>
              <h3 class="skill__label">{group.label}</h3>
              <ul class="chip-row">
                {group.items.map((item) => (
                  <li class="chip">{item}</li>
                ))}
              </ul>
            </div>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Languages + Education -->
  <section class="section">
    <div class="container split">
      <div data-reveal>
        <div class="section-head section-head--sm">
          <p class="eyebrow">Languages</p>
          <h2 class="h2">Three, fluently</h2>
        </div>

        <ul class="langs">
          {
            languages.map((language) => (
              <li class="lang">
                <div class="lang__row">
                  <span class="lang__name">{language.name}</span>
                  <span class="lang__level">{language.level}</span>
                </div>
                {/* Decorative meter — the level text above carries the meaning. */}
                <span class="lang__meter" aria-hidden="true">
                  <span class="lang__fill" style={`--pct:${language.proficiency}%`} />
                </span>
              </li>
            ))
          }
        </ul>
      </div>

      <div data-reveal style="--reveal-delay:90ms">
        <div class="section-head section-head--sm">
          <p class="eyebrow">Education</p>
          <h2 class="h2">Where it started</h2>
        </div>

        {
          education.map((entry) => (
            <article class="edu card">
              {entry.logo && (
                <div class="edu__logo">
                  <img
                    src={url(entry.logo)}
                    alt={entry.logoAlt ?? `${entry.institution} logo`}
                    loading="lazy"
                    decoding="async"
                    width="180"
                    height="64"
                  />
                </div>
              )}
              <div class="edu__body">
                <h3 class="edu__school">
                  {entry.institutionUrl ? (
                    <a
                      class="link-underline"
                      href={entry.institutionUrl}
                      target="_blank"
                      rel="noopener noreferrer"
                    >
                      {entry.institution}
                    </a>
                  ) : (
                    entry.institution
                  )}
                </h3>
                <p class="edu__degree">
                  {entry.degree} — {entry.field}
                </p>
                <p class="edu__period">{entry.period}</p>
              </div>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <!-- Contact -->
  <section class="section section--tight">
    <div class="container">
      <div class="contact" data-reveal>
        <span class="contact__dots dots" aria-hidden="true"></span>

        <div class="contact__copy">
          <p class="eyebrow eyebrow--light">Contact</p>
          <h2 class="h2">Say hello</h2>
          <p class="contact__text">
            LinkedIn is the best place to reach me — for roles, freelance QA, or just to
            talk about how something broke.
          </p>
        </div>

        <div class="contact__actions">
          <a
            class="btn"
            href={links.linkedin.href}
            target="_blank"
            rel="noopener noreferrer"
          >
            <svg width="16" height="16" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
              <path
                d="M4.98 3.5a2.5 2.5 0 1 1 0 5 2.5 2.5 0 0 1 0-5ZM3 9h4v12H3zM9 9h3.8v1.7h.05c.53-.95 1.83-1.95 3.77-1.95 4.03 0 4.78 2.5 4.78 5.76V21h-4v-5.6c0-1.34-.03-3.06-1.9-3.06-1.9 0-2.2 1.45-2.2 2.96V21H9z"
              ></path>
            </svg>
            {links.linkedin.display}
          </a>
          <p class="contact__where">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
              <path d="M12 21s7-5.5 7-11a7 7 0 1 0-14 0c0 5.5 7 11 7 11Z"></path>
              <circle cx="12" cy="10" r="2.6"></circle>
            </svg>
            Based in {site.location}
          </p>
        </div>
      </div>
    </div>
  </section>
</Base>

<style>
  /* -------------------------------------------------------------- bio -- */
  .bio {
    display: grid;
    gap: clamp(1.75rem, 1rem + 4vw, 3.5rem);
    align-items: center;
  }

  @media (min-width: 52rem) {
    .bio {
      grid-template-columns: minmax(0, 0.75fr) minmax(0, 1.25fr);
    }
  }

  .bio__portrait {
    position: relative;
    inline-size: min(100%, 20rem);
    justify-self: center;
    aspect-ratio: 4 / 5;
    overflow: hidden;
    border: 1px solid var(--c-line);
  }

  .bio__photo {
    inline-size: 100%;
    block-size: 100%;
    object-fit: cover;
  }

  .bio__placeholder {
    position: relative;
    display: grid;
    place-content: center;
    justify-items: center;
    gap: 0.75rem;
    block-size: 100%;
    background: linear-gradient(
      160deg,
      var(--c-paper),
      color-mix(in srgb, var(--c-purple) 8%, var(--c-paper))
    );
  }

  .bio__dots {
    position: absolute;
    inset: 0;
    --dot-color: rgb(47 45 46 / 14%);
    --dot-gap: 16px;
  }

  .bio__monogram {
    font-size: clamp(4rem, 3rem + 6vw, 6rem);
    font-weight: 900;
    line-height: 1;
    letter-spacing: -0.04em;
    color: transparent;
    /* Mid-grey rather than the hairline colour, so the placeholder reads as a
       deliberate panel instead of a failed image. */
    -webkit-text-stroke: 2px #a9a6a7;
  }

  .bio__caption {
    font-size: var(--fs-xs);
    font-weight: 700;
    letter-spacing: 0.24em;
    text-transform: uppercase;
    color: var(--c-muted);
  }

  .bio__copy > * + * {
    margin-block-start: var(--space-md);
  }

  .bio__text {
    --flow: var(--space-md);
    max-inline-size: var(--maxw-prose);
    color: var(--c-muted);
  }

  .bio__copy .btn {
    margin-block-start: var(--space-lg);
  }

  /* ------------------------------------------------------------ skills -- */
  .skill {
    gap: var(--space-sm);
    padding: clamp(1.1rem, 0.9rem + 0.8vw, 1.6rem);
    block-size: 100%;
  }

  .skill__icon {
    display: grid;
    place-items: center;
    inline-size: 2.5rem;
    block-size: 2.5rem;
    background: var(--grad-brand-diag);
    color: var(--c-white);
  }

  .skill__label {
    font-size: var(--fs-sm);
    font-weight: 800;
    letter-spacing: 0.06em;
    text-transform: uppercase;
  }

  /* ----------------------------------------------- languages + education -- */
  .split {
    display: grid;
    gap: clamp(2.5rem, 1.5rem + 4vw, 4rem);
  }

  @media (min-width: 52rem) {
    .split {
      grid-template-columns: 1fr 1fr;
      align-items: start;
    }
  }

  .section-head--sm {
    margin-block-end: var(--space-xl);
  }

  .langs {
    display: grid;
    gap: var(--space-lg);
  }

  .lang__row {
    display: flex;
    flex-wrap: wrap;
    align-items: baseline;
    justify-content: space-between;
    gap: var(--space-xs);
    margin-block-end: var(--space-xs);
  }

  .lang__name {
    font-size: var(--fs-h3);
    font-weight: 800;
  }

  .lang__level {
    font-size: var(--fs-xs);
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--c-muted);
  }

  .lang__meter {
    display: block;
    block-size: 6px;
    background: var(--c-paper);
    border: 1px solid var(--c-line);
  }

  .lang__fill {
    display: block;
    block-size: 100%;
    inline-size: var(--pct);
    background: var(--grad-brand);
  }

  .edu {
    flex-direction: row;
    align-items: center;
    gap: var(--space-md);
    padding: clamp(1.1rem, 0.9rem + 0.8vw, 1.5rem);
  }

  .edu__logo {
    display: grid;
    place-items: center;
    flex: none;
    inline-size: 5rem;
    block-size: 3.5rem;
    padding: 0.35rem;
    background: var(--c-paper);
    border: 1px solid var(--c-line);
  }

  .edu__logo img {
    max-block-size: 100%;
    inline-size: auto;
    object-fit: contain;
  }

  .edu__school {
    font-size: var(--fs-h3);
    line-height: 1.2;
  }

  .edu__degree {
    font-size: var(--fs-sm);
    color: var(--c-muted);
  }

  .edu__period {
    margin-block-start: var(--space-2xs);
    font-size: var(--fs-xs);
    font-weight: 700;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--c-gold);
  }

  /* ----------------------------------------------------------- contact -- */
  .contact {
    position: relative;
    overflow: hidden;
    display: grid;
    gap: var(--space-xl);
    align-items: center;
    padding: clamp(1.75rem, 1.2rem + 3vw, 3.25rem);
    background: var(--grad-brand-diag);
    color: var(--c-white);
  }

  @media (min-width: 48rem) {
    .contact {
      grid-template-columns: minmax(0, 1.4fr) minmax(0, 1fr);
    }
  }

  .contact__dots {
    position: absolute;
    inset: 0;
    --dot-color: rgb(255 255 255 / 22%);
    --dot-gap: 16px;
    mask-image: radial-gradient(90% 120% at 100% 0%, #000, transparent 65%);
  }

  .contact__copy {
    position: relative;
  }

  .contact__copy > * + * {
    margin-block-start: var(--space-sm);
  }

  .contact__text {
    max-inline-size: 44ch;
    color: rgb(255 255 255 / 88%);
  }

  .contact__actions {
    position: relative;
    display: grid;
    gap: var(--space-md);
    justify-items: start;
  }

  @media (min-width: 48rem) {
    .contact__actions {
      justify-items: end;
    }
  }

  .contact__actions .btn {
    --btn-bg: var(--c-white);
    --btn-fg: var(--c-black);
  }

  .contact__actions .btn:hover {
    --btn-bg: var(--c-gold);
    --btn-fg: var(--c-black);
  }

  .contact__where {
    display: flex;
    align-items: center;
    gap: 0.45rem;
    font-size: var(--fs-xs);
    font-weight: 600;
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: rgb(255 255 255 / 72%);
  }
</style>
